"""Triple-tap resistance breakout (15m).

A horizontal level touched by THREE visible, sharp swing highs on separate days
(16h-4d apart) at the same price (~N*ATR of the last retrace), with the
pullbacks tightening into the level (R1 >= R2 >= R3 and 0.5*R1 < R2 <= R1).
After the third tap price consolidates under the high; on a volume regime STEP
(____ -> ПППП on the 4h chart) we buy the break of the level, stop under the
consolidation, target H1 + the height of the first retrace (measured move).

Stage 1 here is DETECTION + eyeball chart export only. No EV claims until the
setups survive visual QA (validity-before-EV, per project memory).
"""
