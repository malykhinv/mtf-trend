# Крипто-стратегия: итоговая однозначная инструкция (без кода)

## 1. Технологии и базовые правила
- Язык: Python. Строгая типизация: `mypy --strict`. Использовать только `@dataclass`, `Enum`, `TypedDict` (TypedDict — только на границе парсинга внешних JSON).
- CCXT, одна биржа за запуск (`config.EXCHANGE`).
- Таймзона всех времён: `Europe/Belgrade`.
- Маржа: `isolated`. Режим: `single` (без хеджирования).
- Комиссии/проскальзывание: **не учитывать** в любых расчётах (лайв и бэктест).
- Вся конфигурация — в `config/config.py` и `.env`. Никакой рефлексии (`hasattr/getattr/isinstance/try import`). В домене — никаких словарей.
- Логи: русский, формат `ЧЧ:ММ:СС Сообщение`.
- На старте: `set_leverage(LEVERAGE, MARGIN_MODE)`. При отказе — только лог, без повторов/эскалации.
- Баланс и маржа: USDT.
- Размер позиции: `max(POSITION_FRACTION * balance, POSITION_MIN_USDT)` с округлением к `lotSize`. Ограничение риска по стоп-дистанции не применяется.
- Источник цены для пробоя: `Last`. Спред игнорируется. Гэп через уровень — вход как обычно.
- Источники точностей (`tickSize`, `stepSize`, `minQty`, `minNotional`) обязательны, округления — в `utils`, детерминированы.

## 2. Получение данных (REST)
- Всегда REST, WebSocket **не использовать**.
- Последовательность: получить список фьючерсных символов → отфильтровать → для каждого символа выполнять REST‑проход по старшему TF (H).
- При активации слежения по символу — продолжать REST по H и L только для него; прочие символы продолжают опрос на H без L.

## 3. Таймфреймы
- Разрешённые пары: `(H, L) ∈ {(5m, 1m), (1m, 10s)}` (фиксируется в конфиге).
- Все индикаторы — на **закрытых** свечах. Пробой уровня — по тикам `Last` **без** ожидания закрытия свечи.

## 4. Фильтрация символов
- Для каждого фьючерса определить «возраст листинга».
  - Листинг ≤ 14 дней: `avg_daily_volume_usdt > 5_000_000`.
  - Листинг > 14 дней: одновременно `avg_daily_volume_usdt > 50_000_000` и `trades_24h > 500_000`.
- `avg_daily_volume_usdt` — среднее по D1 за `N_AVG_DAYS`. `trades_24h` — из биржевого статуса/тикера. Внешние структуры → `TypedDict` → доменные модели.

## 5. Индикаторы и определения
### 5.1 Нормализованный ATR на H (`ATR_H`)
- Истинный `TR = max(high-low, |high-close_prev|, |low-close_prev|)`.
- Нормализация выбросов: клиппировать `TR` по перцентилям `[P_LOW, P_HIGH]` из окна `ATR_PERIOD*WINSOR_MULT`.
- `ATR_H = EMA(TR_clipped, ATR_PERIOD)`. Параметры в конфиге.

### 5.2 Аномальная зелёная свеча `C*` (pump) на H
- Бычья (`close > open`) и `body = close - open ≥ ATR_MULT * ATR_H`, где `ATR_MULT = 5` (фиксировано).

### 5.3 Главный хай `H_main`
- `H_main = high(C*)` и это максимум за **последние 7 календарных дней** (включая `C*`). Если нет — сетап не рассматривается.

### 5.4 Коррекция после `C*`
- `L_pullback` — минимум на H после закрытия `C*`.
- Условие валидности: `(H_main - L_pullback) / body ≤ 0.40`. Нарушение отменяет сценарий.

### 5.5 Свинг‑хаи на L
- Определяются модулем **Swings** (алгоритм будет предоставлен позднее). Интерфейс зафиксирован (см. §9).

### 5.6 Проторговка
- Диапазон `Band = [b_low, b_high]` на L, где подряд не менее `CONSOL_MIN_BARS` закрытых свечей имеют `high ≤ b_high` и `low ≥ b_low`.
- Ширина `b_high - b_low < ATR_H`.

## 6. Высокий объём на H
- Окно: последние `N_VOL_H` свечей на H.
- `z = (Vol_t - mean(Vol, N_VOL_H)) / std(Vol, N_VOL_H)`.
- Условие допуска: `z ≥ 5.0`. При `std=0` — пропуск символа до следующей проверки.

## 7. Сценарии отмены до входа
- Углубление коррекции > 40% тела `C*` — отмена сценария.
- Перехай `H_main` — отмена сценария.
- Проверка по закрытым свечам H. Повторные входы разрешены при новом выполнении всех условий.

## 8. Формирование уровня и сигнал
1) На L собрать множество свингов `S` после коррекции (интервал от закрытия `C*`).
2) Допустимы два шаблона:
   - **(А)** `|S| ≥ 2` (два и более свинг‑хая).
   - **(Б)** `|S| = 1` **и** есть проторговка вокруг этого хая (см. §5.6).
3) Верх уровня `level_top`:
   - (А) `max(price ∈ S)`; (Б) цена единственного хая.
4) Низ уровня `level_low`:
   - (А) `min(price ∈ S)`; (Б) `b_low` из проторговки.
5) Узость: `level_top - level_low < ATR_H`.
6) Ограничение сверху: над `level_top` допускается **не более одного** свинг‑хая (не считая `H_main`). Если такой свинг появляется до пробоя — пересобрать уровень.
7) Пробой: факт `Last ≥ level_top` (без объёма и без закрытия). Гэп не учитывается.

## 9. Контракты доменных модулей (без кода)
- `detect_high_volume_on_H(ohlcv_H) -> bool`
- `detect_pump_candle_on_H(ohlcv_H, ATR_H) -> Pump(c_star, H_main)`
- `validate_pullback_on_H(ohlcv_H_after_c_star) -> tuple[bool, L_pullback]`
- `extract_swings_on_L(ohlcv_L_since_c_star) -> SwingsOutput(swings: list[SwingHigh], has_consolidation: bool, consolidation_band: Band|None)`
- `build_level(swings_output, ATR_H) -> Level(level_low, level_top, width)` (строгая проверка §8.2–8.6)
- `check_breakout(last_price, level_top) -> bool`
- `compute_rr(entry, L_pullback, H_main) -> float` где `RR = (H_main - entry) / (entry - L_pullback)`

## 10. Правила входа, RR, цели, MIN_TP
- Вход `Entry`: первый момент `Last ≥ level_top`, если выполнены одновременно:
  - `RR > RR_MIN` (по умолчанию `RR_MIN = 1.0`),
  - **MIN_TP:** `(TP1 - Entry) / Entry ≥ 0.01` (минимум 1% от цены входа до первой цели).
- Стоп `Stop`: `L_pullback` (округление к `tickSize` + `STOP_TRIGGER`).
- Цели:
  - `TP1 = H_main` (допускается +`tickSize` для перехая). Объём выхода: 50%.
  - `TP2 = H_main + (2/3) * (H_main - L_pullback)`. Объём выхода: 50%.
- Перевод стопа в БУ при исполнении TP1: `Stop := (Entry + TP1) / 2`, затем округлить к `tickSize` и применить `STOP_TRIGGER`.
- Запрет на новые сделки по символу при наличии открытой позиции (локальный флаг до закрытия).

## 11. Порядок по символу (H→L→сигнал→сделка)
1) На H: фильтры символов (§4).
2) На H: `z ≥ 5.0`.
3) На H: `C*` с `body ≥ 5*ATR_H`, её `high` — максимум 7 дней.
4) На H: валидная коррекция (`≤ 40%`).
5) На L: собрать `S`, проверить шаблон (А/Б), узость и ограничение сверху.
6) При `Last ≥ level_top`: вычислить `RR` и проверить `MIN_TP`. Если условия выполнены — выставить Entry, SL, TP1, TP2.

## 12. Ордеры и события
- Размещать: Entry, SL, TP1, TP2. Частичное исполнение обрабатывать штатно (без спец‑логики).
- При исполнении TP1: перевести стоп по формуле БУ и отправить уведомление.
- При исполнении SL или TP2: закрыть позицию и уведомить.
- При любой ошибке размещения/модификации ордеров (цена вне диапазона, недостаточная маржа, `reduceOnly` и др.): отправить сообщение в Telegram, записать в лог, установить «кулдаун» на символ **5 минут** (в течение кулдауна не инициировать новые входы).

## 13. Telegram‑уведомления (после факта)
- «Ордер размещён»: символ, пара TF, Entry/SL/TP1/TP2, RR, размер позиции.
- «TP1 исполнен»: символ, цена, время, новый стоп (БУ).
- «TP2 исполнен» или «SL исполнен»: символ, цена, время.
- «Отмена сценария без входа»: причина — «глубокая коррекция» или «перехай».
- «Ошибка ордера»: код/причина, установлен кулдаун 5 минут.

## 14. Состояния и переменные
- Пер‑символьные флаги: `has_open_position`, `cooldown_until`, последние `H_main`, `L_pullback`, `level_top/low`, текущий статус сценария.
- Персистентность на диск не требуется. После рестарта состояние собирается заново.

## 15. Бэктест‑слой
### 15.1 Ввод
- Внутри `.py`‑файла задать: `symbol`, `start_datetime` (Europe/Belgrade), выбранную пару TF.

### 15.2 Данные
- Исторический `OHLCV` на H и L (REST или локальные файлы), идентичные лайву по структуре и округлениям.
- Та же нормализация ATR, те же правила z‑score, коррекции и уровней.

### 15.3 Проход и исход
- Идти по H слева‑направо, бар за баром. На каждом закрытом баре H:
  - Вычислить `ATR_H`, проверить объём `z`, распознать `C*`, проверить 7‑дневный максимум, валидировать коррекцию.
  - При выполнении условий перейти к анализу L в интервале от закрытия `C*`: собрать свинги/проторговку, построить уровень, дождаться первого `Last ≥ level_top`.
  - В момент входа: рассчитать `RR` и проверить `MIN_TP`. При `RR ≤ RR_MIN` или `(TP1-Entry)/Entry < 0.01` — вход отменяется.
  - После входа определить, что достигается раньше (с округлениями к `tickSize`): SL, TP1 (с переносом стопа в БУ по формуле), TP2.
  - При одновременном касании цен порядок фиксирован: **SL → TP1 → TP2**.

### 15.4 Лог бэктеста
- Печатать: `H_main`, `L_pullback`, `level_low/top/width`, список свингов (цена/время), тип сетапа (А/Б), Entry/SL/TP1/TP2, `RR`, проверку `MIN_TP`, исход (`TP1+BU`, `TP1+TP2`, `SL`).

## 16. Логи (минимум)
- Детект `C*`: параметры свечи, `ATR_H`, множитель.
- `H_main` и проверка «максимум 7 дней».
- Коррекция: `L_pullback`, глубина (% тела), вердикт.
- Уровень: тип (А/Б), `level_low/top`, ширина, ограничение сверху.
- Пробой, Entry, `RR`, проверка `MIN_TP`.
- Ордерные события и перенос стопа.
- Отмены и кулдауны.

## 17. Конфигурация (минимум ключей)
- Таймфреймы: `HTF`, `LTF`.
- ATR: `ATR_PERIOD`, `ATR_MULT=5`, `WINSOR_MULT`, `P_LOW`, `P_HIGH`.
- Объёмы: `N_VOL_H`, `Z_MIN=5.0`.
- Коррекция: `MAX_PULLBACK_FRACTION=0.40`.
- Уровень: `CONSOL_MIN_BARS`, `BAND_MAX_MODE='ATR_H'`.
- RR: `RR_MIN=1.0`.
- MIN_TP: `MIN_TP_PCT=0.01` (жёсткое требование к `(TP1-Entry)/Entry`).
- Ордера: `STOP_TRIGGER`, стратегии округления к `tickSize/stepSize`.
- Размер позиции: `POSITION_FRACTION`, `POSITION_MIN_USDT`.
- Скан: `MARKET_SCAN_INTERVAL_H`.
- Данные: флаги REST‑источников для H и L.
- Фильтр символов: `N_AVG_DAYS` и пороги объёма/сделок.
- Telegram: токен, chat_id, формат.
- Прочее: `LEVERAGE`, `MARGIN_MODE='isolated'`, `BREAKOUT_PRICE_SOURCE='Last'`, `COOLDOWN_MINUTES=5`.
- Ключи API: грузить из `.env` (скоупы по умолчанию).

---

Этот документ однозначно определяет стратегию, правила входа/выхода, обработку ошибок, логи и бэктест. Реализация «Swings» подключается без изменения интерфейсов.
