Drop generated `.patch` files in this directory.

`tools/apply_latest_patch.py` applies the newest `*.patch` by file modification time and uses the patch filename without `.patch` as the git commit message.
