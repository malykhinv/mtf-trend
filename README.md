# MTF Trend

This project uses Binance API credentials provided via environment variables.

Edit `config/.env` and set your keys:

```
BINANCE_API_KEY=<your_key>
BINANCE_API_SECRET=<your_secret>
```

The committed file includes empty placeholders to avoid accidentally sharing secrets. Replace the values locally and **do not commit your real credentials**.
